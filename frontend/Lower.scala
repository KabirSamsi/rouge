package frontend

object Lower {
    /*  Construct a clean lowering pipeline from a parsed AST into a family analysis-ready CFGs.
        Also constructs a class and function table, as well as a call graph.
    */
    def constructGraph(Node : Ast.Function) : Ast.Function = {

    }
}